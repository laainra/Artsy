import 'package:flutter/material.dart';

class Home extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Home'),
      ),
      body: ListView(
        children: [
          // NewWorksSection(),
          // ExploreSection(),
          // AuctionSection(),
          // ArtAdvisorSection(),
          // LatestAuctionResult(),
          // GalleriesSection(),
          // ArtsyEditorialSection(),
          // CuratorsPickSection(),
          // CollectionsSection(),
          // TrendingArtistSection(),
          // RecentlyViewedSection(),
          // ViewingRoomsSection(),
          // ShowsForYouSection(),
          // FeaturedFairsSection(),
        ],
      ),
    );
  }
}
